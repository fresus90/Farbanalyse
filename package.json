{
  "name": "farbanalyse",
  "version": "1.0.0",
  "description": "Persönliche Farbanalyse – Finde deinen Farbtyp",
  "type": "module",
  "scripts": {
    "dev": "npm run assets && vite",
    "build": "npm run assets && vite build",
    "preview": "vite preview",
    "assets": "npm run assets:mediapipe && npm run assets:model",
    "assets:mediapipe": "node scripts/sync-mediapipe-assets.mjs",
    "assets:model": "node scripts/fetch-models.mjs",
    "postinstall": "node scripts/sync-mediapipe-assets.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^1.3.0"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}
